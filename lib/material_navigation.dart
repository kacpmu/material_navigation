/// Material 3 Expressive navigation components.
///
/// A small set of navigation widgets that move with the Material 3 Expressive
/// motion system:
///
///  * [NavigationBar] — an adaptive bottom navigation bar.
///  * [NavigationRail] — a rail that collapses and expands, in place or as an
///    optional modal overlay.
///  * [NavigationDestination] — a destination that morphs between the bar's and
///    rail's layouts.
///  * [NavigationMotion] — the shared physics (spring) or curve motion the bar
///    and rail animate with.
library;

export 'src/navigation_bar.dart';
export 'src/navigation_destination.dart';
export 'src/navigation_motion.dart';
export 'src/navigation_rail.dart';
